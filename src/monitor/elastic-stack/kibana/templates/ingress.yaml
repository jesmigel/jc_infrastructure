---
# Source: kibana/templates/ingress.yaml
apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: kibana-7-8-0-kibana
  labels: 
    app: kibana
    release: "kibana-7-8-0"
    heritage: Helm
spec:
  rules:
    - host: elk.local-1.vm
      http:
        paths:
          - path: /
            backend:
              serviceName: kibana-7-8-0-kibana
              servicePort: 5601
